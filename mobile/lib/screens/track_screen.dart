import 'package:flutter/material.dart';

/// Owner: S1. Placeholder — real status tracking + QR check-in lands with Requests & Workflow.
class TrackScreen extends StatelessWidget {
  const TrackScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Track your requests'));
  }
}
